# Versoes fixadas nas que a suite exercita. O job docker do CI valida o
# gunicorn, que os testes nao tocam.
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
# 8.1.8 e anteriores tem PYSEC-2026-2132 (injecao de comando); corrigido em
# 8.3.3. O pip-audit do CI pegou isso.
click==8.4.2
blinker==1.9.0
Pillow==12.3.0
pypdf==6.16.1
flasgger==0.9.7.1
flask-limiter==4.1.1
# Cliente do contador compartilhado de rate limit. O `limits` so entende
# redis:// se este pacote existir.
redis==8.1.0
gunicorn==26.1.0
